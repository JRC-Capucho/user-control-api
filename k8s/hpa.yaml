apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler

metadata:
  name: user-control-api-hpa

spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-control-api
  minReplicas: 3
  maxReplicas: 8
  targetCPUUtilizationPercentage: 75
